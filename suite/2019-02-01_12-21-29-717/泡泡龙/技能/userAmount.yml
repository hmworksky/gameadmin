-   config:
        name: userAmount
        request:
            base_url: ws://tst-bubble-stg126.1768.com/primus?userId=
        variables:
        -   userId: '2038983921'
-   test:
        name: getUserInfo
        request:
            data:
                cmd: getUserInfo
                params: '{"userId":$userId}'
            method: PRIMUS
            url: ws://tst-bubble-stg126.1768.com/primus?userId=$userId
        validate:
        -   check: content.code
            comparator: equals
            expected: 200
-   test:
        name: userAmount
        request:
            data:
                cmd: userAmount
                params: '{"userId":$userId}'
            method: PRIMUS
            url: $userId
        validate:
        -   check: content.code
            comparator: equals
            expected: 200
